// import { useState } from "react";

// function useParse(jsonString: string): null {
//   const [parsedData, setParsedData] = useState(null);
//   if (!jsonString) return null;

//   try {
//     const note = JSON.parse(jsonString);
//   } catch (error) {
//     console.error("Failed to parse JSON string with error: ", error);
//     return null;
//   }
// }

// export default useParse;

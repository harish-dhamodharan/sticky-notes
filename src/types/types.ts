export type AvailableColors = "color-purple" | "color-blue" | "color-yellow";

export type Position = { x: number; y: number };
export type Colors = {
  id: string;
  colorHeader: string;
  colorBody: string;
  colorText: string;
};
